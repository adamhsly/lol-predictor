services:
  postgres:
    image: postgres:18-alpine
    container_name: lol-genius-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: lol_genius
      POSTGRES_USER: lol_genius
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lol_genius_dev}
    volumes:
      - pgdata:/var/lib/postgresql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lol_genius"]
      interval: 10s
      timeout: 5s
      retries: 5

  riot-proxy:
    build:
      context: .
      dockerfile: Dockerfile.proxy
    container_name: lol-genius-proxy
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      - LOL_GENIUS_REGION=na1
      - LOL_GENIUS_ROUTING=americas
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/riot/v1/health"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  migrate:
    build: .
    container_name: lol-genius-migrate
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgresql://lol_genius:${POSTGRES_PASSWORD:-lol_genius_dev}@postgres:5432/lol_genius
    command: ["init-db", "--wait"]

  crawler:
    build: .
    container_name: lol-genius-crawler
    restart: unless-stopped
    depends_on:
      riot-proxy:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    volumes:
      - model-data:/app/data/models
      - ./data/ddragon:/app/data/ddragon
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - LOL_GENIUS_DOCKER=1
      - RIOT_PROXY_URL=http://riot-proxy:8080
      - DATABASE_URL=postgresql://lol_genius:${POSTGRES_PASSWORD:-lol_genius_dev}@postgres:5432/lol_genius
    command: ["crawl"]
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  dashboard-api:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    container_name: lol-genius-dashboard-api
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
    volumes:
      - model-data:/app/data/models
      - ./data/ddragon:/app/data/ddragon
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - DATABASE_URL=postgresql://lol_genius:${POSTGRES_PASSWORD:-lol_genius_dev}@postgres:5432/lol_genius
      - PROXY_URL=http://riot-proxy:8080
      - MODEL_DIR=data/models
      - DDRAGON_CACHE=data/ddragon
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8081/api/v1/model/training-status')"]
      interval: 30s
      timeout: 10s
      start_period: 15s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  dashboard:
    build:
      context: ./frontend
    container_name: lol-genius-dashboard
    restart: unless-stopped
    depends_on:
      dashboard-api:
        condition: service_healthy
    ports:
      - "3000:80"

volumes:
  pgdata:
  model-data:
