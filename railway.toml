[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m pip install -r requirements-web.txt && python -m lol_genius.dashboard.run"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
